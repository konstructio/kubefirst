version: "3"

services:
  kubefirst-tests:
    build:
      context: .
      dockerfile: ./build/Dockerfile.ci
    working_dir: /home/developer/kubefirst
    platform: linux/amd64
    volumes:
      - ./:/home/developer/kubefirst
    command: sh -c "task unit-test-with-report"
